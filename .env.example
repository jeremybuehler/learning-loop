# Database (Postgres)
# Example (Neon/Vercel Postgres pooled connection):
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?pgbouncer=true&connection_limit=1"
DATABASE_URL=""

# API key for write endpoints
LL_API_KEY=""

# Basic rate limiting (per IP)
# Default window: 60 seconds; telemetry limit defaults to 60, feedback to 30 when unset.
LL_RL_WINDOW_MS="60000"
LL_RL_TELEMETRY_LIMIT="60"
LL_RL_FEEDBACK_LIMIT="30"

